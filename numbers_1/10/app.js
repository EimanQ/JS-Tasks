const functA = +prompt(`Введите переменную a `);
const functB = +prompt(`Введите переменную b `);

console.log(`Значение функции f(a,b) = ${3 * (functA + functB) ** 3 + 275 * functB ** 2 - 127 * functA - 41}`);
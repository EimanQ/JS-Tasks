const valueNumber = +prompt(`Введите целое число `);

console.log(`Следующее число за числом ${valueNumber} число ${valueNumber + 1}`);
console.log(`Предыдущее число за числом ${valueNumber} число ${valueNumber - 1}`);
